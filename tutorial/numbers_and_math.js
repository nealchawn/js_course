const num1 = 100;
const num2 = 50;
let val;

// Simple math withb numbers
val = num1 + num2;
val = num1 - num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2;
val = num1**2;

// Math Object
val = Math.PI;
val = (Math.E);
//val = Math.round(2.4);
//val = Math.ceil(2.4);
//val = Math.floor(2.6);
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.pow(8,2);
val = Math.min(2,3,4,5);
val = Math.max(2,3,4,5);


val = Math.random(); // random decimal
val = Math.floor(Math.random() * 20 +1); // 1-20


console.log(val);